{
  "name": "unveiled",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @unveiled/web dev",
    "build": "bun --filter @unveiled/web build",
    "deploy:workers": "bunx wrangler deploy --config apps/web/wrangler.toml",
    "secrets:workers": "bun scripts/set-worker-secrets.ts",
    "lint": "biome check .",
    "typecheck": "bun run --filter '*' typecheck",
    "db:generate": "bun --filter @unveiled/db generate",
    "db:migrate": "bun --filter @unveiled/db migrate",
    "seed:demo": "bun scripts/seed-demo.ts",
    "seed:admin-pagination": "bun scripts/seed-admin-pagination.ts",
    "stories": "bun --filter @unveiled/ui stories & bun --filter @unveiled/web stories",
    "test:e2e": "bunx playwright test --config e2e/playwright.config.ts"
  },
  "catalog": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "drizzle-orm": "^0.40.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@unveiled/db": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@playwright/test": "^1.49.1",
    "@unveiled/config": "workspace:*",
    "playwright": "^1.49.1",
    "wrangler": "^4.14.0"
  }
}
